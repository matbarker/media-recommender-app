version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: media-recommender
    ports:
      - "3002:3000"
    volumes:
      - ./data:/app/data
      - ./.env.local:/app/.env.local
    environment:
      - PORT=3000
    restart: unless-stopped
