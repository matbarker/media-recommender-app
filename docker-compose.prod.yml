version: '3.8'

services:
  app:
    image: ghcr.io/yourusername/media-recommender-app:latest
    container_name: media-recommender-prod
    ports:
      - "3002:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env.production
    restart: always
